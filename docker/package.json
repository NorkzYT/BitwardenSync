{
  "name": "bitwardensync-runtime",
  "description": "Container runtime for BitwardenSync: the verified vault purge tool.",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "hash-wasm": "^4.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.18.7",
    "typescript": "^5.8.3"
  }
}
